# Environment Configuration (Copy to .env for local development)
# Currently the app doesn't require environment variables as it runs fully offline
# Add here if you want to extend the app with a backend in the future

EXPO_PUBLIC_API_URL=
EXPO_PUBLIC_APP_ENV=development
